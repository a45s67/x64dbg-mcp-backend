# Third-party notices

Rust dependencies and their exact versions are recorded in `Cargo.lock` and
retain their respective licenses.

The native plugin links to the x64dbg Plugin SDK import libraries and uses the
Jansson ABI supplied by the target x64dbg installation. The repository and
release ZIP do not redistribute x64dbg or `jansson.dll`.

Microsoft Windows system libraries used by the binaries are not redistributed.
